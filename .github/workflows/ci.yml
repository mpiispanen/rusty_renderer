name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-
      
      - name: Cache cargo index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-git-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-git-
      
      - name: Cache cargo build
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-build-target-
      
      - name: Build (debug)
        run: cargo build --verbose
      
      - name: Build (release)
        run: cargo build --release --verbose
      
      - name: Upload debug binary
        uses: actions/upload-artifact@v4
        with:
          name: rusty_renderer-debug-linux
          path: target/debug/rusty_renderer
          if-no-files-found: error
      
      - name: Upload release binary
        uses: actions/upload-artifact@v4
        with:
          name: rusty_renderer-release-linux
          path: target/release/rusty_renderer
          if-no-files-found: error

  test:
    name: Test (Unit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-test-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-test-
      
      - name: Run tests
        run: cargo test --verbose

  test-rendering-vulkan:
    name: Test Vulkan Rendering
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Install Vulkan software renderer (lavapipe)
        run: |
          sudo apt-get update
          sudo apt-get install -y mesa-vulkan-drivers libvulkan1
      
      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-gpu-test-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-gpu-test-
      
      - name: Build release binary
        run: cargo build --release --verbose
      
      - name: Test Vulkan headless rendering (textured cube)
        run: |
          mkdir -p screenshots/vulkan
          RUST_LOG=info ./target/release/rusty_renderer \
            --scene scenes/gltf_textured.toml \
            --backend vulkan \
            --pipeline forward \
            --headless \
            --screenshot screenshots/vulkan/gltf_textured.png
          
          # Verify screenshot was created
          test -f screenshots/vulkan/gltf_textured.png
          test -s screenshots/vulkan/gltf_textured.png
          echo "✅ Vulkan screenshot created successfully"
        env:
          VK_ICD_FILENAMES: /usr/share/vulkan/icd.d/lvp_icd.x86_64.json
      
      - name: Upload Vulkan screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots-vulkan
          path: screenshots/vulkan/
          retention-days: 30

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      
      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-clippy-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-clippy-
      
      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

  format:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      
      - name: Check formatting
        run: cargo fmt --all -- --check

  docs:
    name: Documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-doc-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-doc-
      
      - name: Build documentation
        run: cargo doc --no-deps --verbose
        env:
          RUSTDOCFLAGS: "-D warnings"
  
  # Windows build with DirectX 12 support
  build-windows:
    name: Build (Windows + DirectX 12)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-
      
      - name: Build (debug)
        run: cargo build --verbose
      
      - name: Build (release)
        run: cargo build --release --verbose
      
      - name: Run tests
        run: cargo test --verbose
      
      - name: Build examples
        run: cargo build --release --examples
      
      - name: Test DirectX 12 headless rendering
        run: |
          Write-Host "Testing DirectX 12 backend with textured cube scene"
          mkdir screenshots/directx -Force
          $env:RUST_LOG = "info"
          ./target/release/rusty_renderer.exe `
            --scene scenes/gltf_textured.toml `
            --backend directx `
            --pipeline forward `
            --headless `
            --screenshot screenshots/directx/gltf_textured.png
          
          # Verify screenshot was created
          if (Test-Path screenshots/directx/gltf_textured.png) {
            $fileSize = (Get-Item screenshots/directx/gltf_textured.png).Length
            Write-Host "DirectX screenshot size: $fileSize bytes"
            if ($fileSize -gt 0) {
              Write-Host "✅ DirectX screenshot created successfully"
            } else {
              Write-Host "❌ DirectX screenshot is empty"
              exit 1
            }
          } else {
            Write-Host "❌ DirectX screenshot not found"
            exit 1
          }
        shell: powershell
      
      - name: Upload DirectX screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots-directx
          path: screenshots/directx/
          retention-days: 30
      
      - name: Upload Windows binary
        uses: actions/upload-artifact@v4
        with:
          name: rusty_renderer-windows
          path: target/release/rusty_renderer.exe
  
  # Visual regression testing - compare backends and check against golden images
  visual-regression:
    name: Visual Regression Testing
    runs-on: ubuntu-latest
    needs: [test-rendering-vulkan, build-windows]
    if: always()
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      
      - name: Install FLIP and dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip install flip-evaluator numpy pillow
      
      - name: Download Vulkan screenshots
        uses: actions/download-artifact@v4
        with:
          name: screenshots-vulkan
          path: screenshots/vulkan/
      
      - name: Download DirectX screenshots
        uses: actions/download-artifact@v4
        with:
          name: screenshots-directx
          path: screenshots/directx/
        continue-on-error: true
      
      - name: List all screenshots
        run: |
          echo "📸 Available screenshots:"
          find screenshots -name "*.png" -exec ls -lh {} \;
      
      - name: Compare Vulkan vs DirectX (Backend Parity)
        id: backend_parity
        run: |
          echo "🔍 Comparing backend outputs..."
          
          # Create comparison directory
          mkdir -p comparisons/backend_parity
          
          # Compare if both screenshots exist
          if [ -f "screenshots/vulkan/gltf_textured.png" ] && [ -f "screenshots/directx/gltf_textured.png" ]; then
            python3 scripts/flip_compare.py \
              screenshots/vulkan/gltf_textured.png \
              screenshots/directx/gltf_textured.png \
              comparisons/backend_parity/vulkan_vs_directx.png \
              --threshold 0.05 || {
                echo "::warning::Backend parity test failed - outputs differ significantly"
                echo "parity_failed=true" >> $GITHUB_OUTPUT
              }
          else
            echo "⚠️  Missing screenshots - skipping backend comparison"
          fi
      
      - name: Compare against golden references
        id: golden_check
        run: |
          echo "🏆 Comparing against golden reference images..."
          
          mkdir -p comparisons/golden
          
          # Check Vulkan against golden reference
          if [ -f "references/triangle/gltf_textured_vulkan.png" ] && [ -f "screenshots/vulkan/gltf_textured.png" ]; then
            python3 scripts/flip_compare.py \
              references/triangle/gltf_textured_vulkan.png \
              screenshots/vulkan/gltf_textured.png \
              comparisons/golden/vulkan_regression.png \
              --threshold 0.03 || {
                echo "::warning::Vulkan regression detected - output differs from golden reference"
                echo "vulkan_regressed=true" >> $GITHUB_OUTPUT
              }
          else
            echo "ℹ️  No golden reference for Vulkan - skipping"
          fi
          
          # Check DirectX against golden reference
          if [ -f "references/triangle/gltf_textured_directx.png" ] && [ -f "screenshots/directx/gltf_textured.png" ]; then
            python3 scripts/flip_compare.py \
              references/triangle/gltf_textured_directx.png \
              screenshots/directx/gltf_textured.png \
              comparisons/golden/directx_regression.png \
              --threshold 0.03 || {
                echo "::warning::DirectX regression detected - output differs from golden reference"
                echo "directx_regressed=true" >> $GITHUB_OUTPUT
              }
          else
            echo "ℹ️  No golden reference for DirectX - skipping"
          fi
        continue-on-error: true
      
      - name: Generate comprehensive visual report
        run: |
          echo "📊 Generating visual regression report..."
          
          # Prepare all screenshots for report
          mkdir -p report_input
          cp -r screenshots/* report_input/ 2>/dev/null || true
          cp -r comparisons/* report_input/ 2>/dev/null || true
          
          # Generate HTML report
          python3 scripts/generate_visual_report.py \
            report_input/ \
            visual-regression-report.html \
            --temp-dir flip_results || {
              echo "⚠️  Report generation had issues but continuing..."
            }
        continue-on-error: true
      
      - name: Upload comparison results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: visual-regression-results
          path: |
            comparisons/
            visual-regression-report.html
            screenshots/
          retention-days: 30
      
      - name: Check for failures and report
        if: always()
        run: |
          echo "=== Visual Regression Summary ==="
          
          if [ "${{ steps.backend_parity.outputs.parity_failed }}" == "true" ]; then
            echo "❌ Backend parity check FAILED - Vulkan and DirectX outputs differ"
          else
            echo "✅ Backend parity check PASSED"
          fi
          
          if [ "${{ steps.golden_check.outputs.vulkan_regressed }}" == "true" ]; then
            echo "⚠️  Vulkan regression detected"
          fi
          
          if [ "${{ steps.golden_check.outputs.directx_regressed }}" == "true" ]; then
            echo "⚠️  DirectX regression detected"
          fi
          
          echo ""
          echo "📊 View detailed report in the visual-regression-results artifact"
          
          # Fail if backend parity is broken (critical)
          if [ "${{ steps.backend_parity.outputs.parity_failed }}" == "true" ]; then
            echo "::error::Backend parity check failed - backends produce different output!"
            exit 1
          fi
